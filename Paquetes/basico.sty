%%% Fuentes conocidas %%%
\usepackage{fontspec} % Requerido para fuentes personalizadas
\usepackage{unicode-math} % Adicion para fuentes matematicas

%%% Tamaños de letras predeterminadas %%%
% | Comando         | Tamaño en 10pt | Tamaño en **11pt** | Tamaño en 12pt |
% | --------------- | -------------- | ------------------ | -------------- |
% | `\tiny`         | 5pt            | **6pt**            | 6pt            |
% | `\scriptsize`   | 7pt            | **8pt**            | 8pt            |
% | `\footnotesize` | 8pt            | **9pt**            | 10pt           |
% | `\small`        | 9pt            | **10pt**           | 10.95pt        |
% | `\normalsize`   | 10pt           | **11pt**           | 12pt           |
% | `\large`        | 12pt           | **12pt**           | 14.4pt         |
% | `\Large`        | 14.4pt         | **14.4pt**         | 17.28pt        |
% | `\LARGE`        | 17.28pt        | **17.28pt**        | 20.74pt        |
% | `\huge`         | 20.74pt        | **20.74pt**        | 24.88pt        |
% | `\Huge`         | 24.88pt        | **24.88pt**        | 24.88pt        |

\fontspec{Arial}
% \setmainfont{Arimo}[
%   Path = Configuraciones/Fuentes/,
%   Extension = .ttf,
%   UprightFont = *-Regular,
%   ItalicFont = *-Italic,
%   BoldFont = *-Bold,
%   BoldItalicFont = *-BoldItalic,
%   Scale = MatchUppercase,
%   Ligatures = TeX
% ]
\setmainfont{Arial}[
  Scale=MatchUppercase,
  UprightFont=*,
  ItalicFont=* Italic,
  BoldFont=* Bold,
  BoldItalic=* Bold Italic
]


\newfontfamily\fuentealternativa{TeXGyreAdventor}
% Opciones de fuentes segun el paquete
% TeXGyrePagella → Palatino (math)
% TeXGyreTermes → Times New Roman (math)
% TeXGyreBonum → Bookman Old Style (math)
% TeXGyreSchola → Century Schoolbook (math)
% TeXGyreChorus → Monotype Corsiva ()
% TeXGyreHeros → Helvetica / Arial ()
% TeXGyreAdventor → Avant Garde Gothic ()

\setmathfont{Cambria Math}
% Adicionales fuentes matematicas
% Garamond-Math
% Asana-Math
% Cambria Math
% Latin Modern Math
