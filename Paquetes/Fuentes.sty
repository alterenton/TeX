% Requerido para fuentes personalizadas
\usepackage{fontspec}
% Adicion para fuentes matematicas
\usepackage{unicode-math} 

%\fontspec{Arial}
% \setmainfont{Arimo}[
%   Path = Configuraciones/Fuentes/,
%   Extension = .ttf,
%   UprightFont = *-Regular,
%   ItalicFont = *-Italic,
%   BoldFont = *-Bold,
%   BoldItalicFont = *-BoldItalic,
%   Scale = MatchUppercase,
%   Ligatures = TeX
% ]
%\setmainfont{Arial}[
%  Scale=MatchUppercase,
%  UprightFont=*,
%  ItalicFont=* Italic,
%  BoldFont=* Bold,
%  BoldItalic=* Bold Italic
%]
\setmainfont{texgyreheros}

%\newfontfamily\fuentealternativa{TeXGyreAdventor}
% Opciones de fuentes segun el paquete
% TeXGyrePagella → Palatino (math)
% TeXGyreTermes → Times New Roman (math)
% TeXGyreBonum → Bookman Old Style (math)
% TeXGyreSchola → Century Schoolbook (math)
% TeXGyreChorus → Monotype Corsiva ()
% TeXGyreHeros → Helvetica / Arial ()
% TeXGyreAdventor → Avant Garde Gothic ()

%\setmathfont{Cambria Math}
% Adicionales fuentes matematicas
% Garamond-Math
% Asana-Math
% Cambria Math
% Latin Modern Math
